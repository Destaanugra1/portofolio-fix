
  # Create as per request

  This is a code bundle for Create as per request. The original project is available at https://www.figma.com/design/s6CIWj3fYwR7sr8UWjYszX/Create-as-per-request.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  